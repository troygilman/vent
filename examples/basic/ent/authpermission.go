// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/troygilman/vent/examples/basic/ent/authpermission"
)

// AuthPermission is the model entity for the AuthPermission schema.
type AuthPermission struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AuthPermissionQuery when eager-loading is set.
	Edges        AuthPermissionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AuthPermissionEdges holds the relations/edges for other nodes in the graph.
type AuthPermissionEdges struct {
	// Groups holds the value of the groups edge.
	Groups []*AuthGroup `json:"groups,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GroupsOrErr returns the Groups value or an error if the edge
// was not loaded in eager-loading.
func (e AuthPermissionEdges) GroupsOrErr() ([]*AuthGroup, error) {
	if e.loadedTypes[0] {
		return e.Groups, nil
	}
	return nil, &NotLoadedError{edge: "groups"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthPermission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authpermission.FieldID:
			values[i] = new(sql.NullInt64)
		case authpermission.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthPermission fields.
func (_m *AuthPermission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authpermission.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case authpermission.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AuthPermission.
// This includes values selected through modifiers, order, etc.
func (_m *AuthPermission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGroups queries the "groups" edge of the AuthPermission entity.
func (_m *AuthPermission) QueryGroups() *AuthGroupQuery {
	return NewAuthPermissionClient(_m.config).QueryGroups(_m)
}

// Update returns a builder for updating this AuthPermission.
// Note that you need to call AuthPermission.Unwrap() before calling this method if this AuthPermission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AuthPermission) Update() *AuthPermissionUpdateOne {
	return NewAuthPermissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AuthPermission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AuthPermission) Unwrap() *AuthPermission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthPermission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AuthPermission) String() string {
	var builder strings.Builder
	builder.WriteString("AuthPermission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteByte(')')
	return builder.String()
}

// AuthPermissions is a parsable slice of AuthPermission.
type AuthPermissions []*AuthPermission
