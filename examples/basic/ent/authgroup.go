// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/troygilman/vent/examples/basic/ent/authgroup"
)

// AuthGroup is the model entity for the AuthGroup schema.
type AuthGroup struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AuthGroupQuery when eager-loading is set.
	Edges        AuthGroupEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AuthGroupEdges holds the relations/edges for other nodes in the graph.
type AuthGroupEdges struct {
	// Permissions holds the value of the permissions edge.
	Permissions []*AuthPermission `json:"permissions,omitempty"`
	// Users holds the value of the users edge.
	Users []*AuthUser `json:"users,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// PermissionsOrErr returns the Permissions value or an error if the edge
// was not loaded in eager-loading.
func (e AuthGroupEdges) PermissionsOrErr() ([]*AuthPermission, error) {
	if e.loadedTypes[0] {
		return e.Permissions, nil
	}
	return nil, &NotLoadedError{edge: "permissions"}
}

// UsersOrErr returns the Users value or an error if the edge
// was not loaded in eager-loading.
func (e AuthGroupEdges) UsersOrErr() ([]*AuthUser, error) {
	if e.loadedTypes[1] {
		return e.Users, nil
	}
	return nil, &NotLoadedError{edge: "users"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthGroup) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authgroup.FieldID:
			values[i] = new(sql.NullInt64)
		case authgroup.FieldName:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthGroup fields.
func (_m *AuthGroup) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authgroup.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case authgroup.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AuthGroup.
// This includes values selected through modifiers, order, etc.
func (_m *AuthGroup) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryPermissions queries the "permissions" edge of the AuthGroup entity.
func (_m *AuthGroup) QueryPermissions() *AuthPermissionQuery {
	return NewAuthGroupClient(_m.config).QueryPermissions(_m)
}

// QueryUsers queries the "users" edge of the AuthGroup entity.
func (_m *AuthGroup) QueryUsers() *AuthUserQuery {
	return NewAuthGroupClient(_m.config).QueryUsers(_m)
}

// Update returns a builder for updating this AuthGroup.
// Note that you need to call AuthGroup.Unwrap() before calling this method if this AuthGroup
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AuthGroup) Update() *AuthGroupUpdateOne {
	return NewAuthGroupClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AuthGroup entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AuthGroup) Unwrap() *AuthGroup {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthGroup is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AuthGroup) String() string {
	var builder strings.Builder
	builder.WriteString("AuthGroup(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteByte(')')
	return builder.String()
}

// AuthGroups is a parsable slice of AuthGroup.
type AuthGroups []*AuthGroup
