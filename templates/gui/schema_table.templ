package gui

import (
	"fmt"
	"strings"

	"github.com/troygilman/vent/requestctx"
)

type SchemaTableProps struct {
	LayoutProps         LayoutProps
	RouteName           string
	SingularDisplayName string
	PluralDisplayName   string
	Columns             []SchemaTableColumn
	Rows                []SchemaTableRow
	FilterableColumns   []SchemaTableFilterableColumn
	RenderContext       RenderContext
}

type SchemaTableColumn struct {
	Name  string
	Label string
	Type  string
}

type SchemaTableRow struct {
	Cells []SchemaTableCell
}

type SchemaTableCell struct {
	Display string
	LinkURL string
}

type SchemaTableFilterableColumn struct {
	Name  string
	Label string
	Type  string
	Value string
}

func tableFilterSignals(columns []SchemaTableFilterableColumn) map[string]map[string]any {
	filter := make(map[string]any, len(columns))
	for _, column := range columns {
		if column.Type == "bool" {
			switch column.Value {
			case "true":
				filter[column.Name] = true
			case "false":
				filter[column.Name] = false
			default:
				filter[column.Name] = nil
			}
			continue
		}
		filter[column.Name] = column.Value
	}
	return map[string]map[string]any{"filter": filter}
}

func tableFilterActiveExpr(columns []SchemaTableFilterableColumn) string {
	parts := make([]string, len(columns))
	for i, column := range columns {
		if column.Type == "bool" {
			parts[i] = fmt.Sprintf("$filter.%s !== null", column.Name)
			continue
		}
		parts[i] = fmt.Sprintf("$filter.%s !== ''", column.Name)
	}
	return strings.Join(parts, " || ")
}

func tableFilterClearExpr(columns []SchemaTableFilterableColumn) string {
	parts := make([]string, 0, len(columns)+1)
	for _, column := range columns {
		if column.Type == "bool" {
			parts = append(parts, fmt.Sprintf("$filter.%s = null", column.Name))
			continue
		}
		parts = append(parts, fmt.Sprintf("$filter.%s = ''", column.Name))
	}
	parts = append(parts, `@get(location.pathname, {filterSignals: {include: /^filter\./}})`)
	return strings.Join(parts, "; ")
}

func BoolFilterValue(v *bool) string {
	if v == nil {
		return ""
	}
	if *v {
		return "true"
	}
	return "false"
}

templ SchemaTablePage(props SchemaTableProps) {
	{{ schemaPath := fmt.Sprintf("%s%s/", requestctx.MustAdminPath(ctx), props.RouteName) }}
	@Index() {
		@Layout(props.LayoutProps) {
			<div class="page-header">
				<div class="page-title">{ props.PluralDisplayName }</div>
				if props.RenderContext.CanCreate {
					<a href={ templ.SafeURL(schemaPath + "add/") }><button class="btn btn-primary">Add { props.SingularDisplayName }</button></a>
				}
			</div>
			if len(props.FilterableColumns) > 0 {
				<form
					class="table-filters"
					method="get"
					action={ templ.SafeURL(schemaPath) }
					data-signals={ templ.JSONString(tableFilterSignals(props.FilterableColumns)) }
					data-push-url="{include: /^filter\./}"
					data-on:submit__prevent="@get(location.pathname, {filterSignals: {include: /^filter\./}})"
				>
					for _, filter := range props.FilterableColumns {
						<label class="table-filter">
							<span class="table-filter-label">{ filter.Label }</span>
							if filter.Type == "string" {
								<div class="input">
									<input type="text" name={ filter.Name } data-bind={ "filter." + filter.Name } value={ filter.Value } placeholder={ "Filter by " + filter.Label }/>
								</div>
							} else if filter.Type == "bool" {
								<select
									class="select"
									name={ filter.Name }
									data-on:change={ "$filter." + filter.Name + " = el.value === '' ? null : el.value === 'true'" }
								>
									<option value="" selected?={ filter.Value == "" }>All</option>
									<option value="true" selected?={ filter.Value == "true" }>Yes</option>
									<option value="false" selected?={ filter.Value == "false" }>No</option>
								</select>
							} else if filter.Type == "int" {
								<div class="input">
									<input type="number" name={ filter.Name } data-bind={ "filter." + filter.Name } value={ filter.Value } placeholder={ "Filter by " + filter.Label }/>
								</div>
							}
						</label>
					}
					<div class="table-filter-actions">
						<button type="submit" class="btn btn-primary" data-indicator="_indicator">Filter</button>
						<button
							type="button"
							class="btn btn-neutral"
							data-show={ tableFilterActiveExpr(props.FilterableColumns) }
							data-on:click={ tableFilterClearExpr(props.FilterableColumns) }
							data-indicator="_indicator"
						>
							Clear
						</button>
					</div>
				</form>
			}
			<div class="table-container">
				<table class="data-table">
					<thead>
						<tr>
							for _, column := range props.Columns {
								<th>{ column.Label }</th>
							}
						</tr>
					</thead>
					<tbody>
						if len(props.Rows) == 0 {
							<tr>
								<td class="table-empty" colspan={ fmt.Sprintf("%d", len(props.Columns)) }>No data</td>
							</tr>
						} else {
							for _, row := range props.Rows {
								<tr>
									for _, cell := range row.Cells {
										if cell.LinkURL != "" {
											<td>
												<a class="link" href={ templ.SafeURL(cell.LinkURL) }>
													{ cell.Display }
												</a>
											</td>
										} else {
											<td>{ cell.Display }</td>
										}
									}
								</tr>
							}
						}
					</tbody>
				</table>
			</div>
			@Indicator()
		}
	}
}
