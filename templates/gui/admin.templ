package gui

type AdminPageProps struct {
	LayoutProps LayoutProps
}

templ AdminPage(props AdminPageProps) {
	@Index(props.LayoutProps.AdminPath, props.LayoutProps.CSRFToken) {
		@Layout(props.LayoutProps)
	}
}
