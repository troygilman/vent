---
description: Never create test files under vent example application directories
alwaysApply: true
---

# No tests in examples

Never create, keep, or regenerate test files under `examples/` (including `examples/basic/` and any `*_test.go` there).

Example apps are demos only. Put tests in library/package paths outside `examples/` (for example module-root `options_test.go`).

If you find example-app tests on the current branch, delete them.
